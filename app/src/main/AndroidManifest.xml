<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mac.qqcopy">

    <application
        android:allowBackup="true"
        android:logo="@drawable/qqpenguin"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        >
        <!--直接去掉actionbar(限制太多了)-->
        <!--android:icon="@mipmap/ic_launcher"還不知道是要icon還是ogo-->

        <activity android:name=".LoginActivity"
            android:label="my LoginActivity label"
            android:windowSoftInputMode="stateVisible">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--設定為app起始activity-->

        <activity android:name=".SignupActivity"
            android:label="my SignuoActivity label"
            android:windowSoftInputMode="stateVisible">
        </activity>

        <activity android:name=".MyFragmentActivity"
            android:label="my AdapterActivity label">
        </activity>
        <!--放置3個page-->

        <activity android:name=".MainFragment"
            android:label="my MainActivity label">
        </activity>


ai
        <activity android:name=".SearchActivity"
            android:label="my SearchActivity label"
            android:windowSoftInputMode="stateVisible">
        </activity>
        <!--這裡也要宣告有另一個activity-->
        <!--android:windowSoftInputMode="stateVisible"切換過來時就自動顯示鍵盤-->



    </application>

</manifest>